apt-get install redis-server
or
sudo yum install redis
sudo systemctl start redis
sudo systemctl enable redis

sudo pip3 install redis
sudo pip3 install youtube-dl
sudo pip3 install python-telegram-bot
sudo pip3 install configparser

# Serve para upar um arquivo a partir de uma conta
sudo yum install python3-devel
sudo yum install python2-devel
pip3.6 install telegram-upload
pip install telegram-upload
https://github.com/Nekmo/telegram-upload/

(Se der erro no Telegram upload)
Clone o repositório e ponha na home do usuário, 
junto com os scripts do ChatBot.

Use o screen para deixar em um terminal rodando.
sudo yum install epel-release
sudo yum install screen